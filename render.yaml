services:
  - type: web
    name: vrptw-research-nami
    env: docker
    plan: free
    branch: demo-release
    dockerCommand: "uvicorn src.backend.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PORT
        value: 8080
