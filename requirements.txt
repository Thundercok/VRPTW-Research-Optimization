fastapi>=0.116
uvicorn[standard]>=0.35
httpx>=0.28
pydantic>=2.11
firebase-admin>=7.0
python-dotenv>=1.0
python-multipart>=0.0.7

# Security + observability
passlib[argon2]>=1.7.4
argon2-cffi>=23.1
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.0

# <2.4 is numba 0.63.1's ceiling; the V2 sweep + golden fingerprints were
# validated under numpy 2.3.5, so the ceiling must stay above 2.3.
numpy>=1.26,<2.4
pandas>=2.2
scipy>=1.13
numba>=0.60
torch>=2.4
safetensors>=0.4
matplotlib>=3.8
